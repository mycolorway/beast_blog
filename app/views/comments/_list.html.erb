<%= render comments %>

<% if comments.next_page %>
<div class="more-comments">
  <%= link_to t('.more_comments'), post_comments_path(post, page: comments.next_page),
    remote: true, data: {disable_with: t('.loading')}, class: 'link-fetch-comments' %>
</div>
<% end %>
