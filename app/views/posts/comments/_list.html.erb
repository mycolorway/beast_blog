<%= render comments %>

<% if comments.next_page %>
  <%= link_to t('.more_comments'), post_comments_path(post, page: comments.next_page),
    remote: true, data: {disable_with: t('.loading')}, class: 'link-more-comments' %>
<% end %>
